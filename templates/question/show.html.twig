{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
        <div class="head">
        <a href="/"><h1>My Quizz</h1></a>
                <div class="log">
                        <a class="button1" href="/">Home</a>
                </div>
        </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>IdCategorie</th>
                <td>{{ question.idCategorie }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><button class="button2" type="submit">back to list</button></a>

    <a href="{{ path('question_edit', {'id': question.id}) }}"><button class="button2" type="submit">edit</button></a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
