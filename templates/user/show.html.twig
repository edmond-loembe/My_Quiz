{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
        <div class="head">
        <a href="/"><h1>Users</h1></a>
                <div class="log">
                        <a class="button1" href="/">Home</a>
                </div>
        </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><button class="button2" type="submit">back to list</button></a>

    <a href="{{ path('user_edit', {'id': user.id}) }}"><button class="button2" type="submit">edit</button></a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
